// Event: Create

vspeed = -2;      // Move UP automatically (2 pixels per frame)
image_alpha = 1;  // Start fully visible
text_to_show = "+10"; // Default text (we can change this later)
color = c_yellow;     // Default color